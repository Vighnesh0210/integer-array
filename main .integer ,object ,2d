interger array in memory allocation

public class MemoryAllocation {
    public static void main(String[] args) {
        int[] numbers;

        numbers = new int[5];

        numbers[0] = 10;
        numbers[1] = 20;
        numbers[2] = 30;
        numbers[3] = 40;
        numbers[4] = 50;

        System.out.println("Array elements:");
        for (int i = 0; i < numbers.length; i++) {
            System.out.println(numbers[i]);
        }
    }
}







2d array in memory allocation.

public class TwoDArrayMemoryAllocation {
    public static void main(String[] args) {
  
        int[][] matrix;

        matrix = new int[3][3];

        for (int i = 0; i < matrix.length; i++) {
            for (int j = 0; j < matrix[i].length; j++) {
                matrix[i][j] = i + j;
            }
        }

        System.out.println("Matrix elements:");
        for (int i = 0; i < matrix.length; i++) {
            for (int j = 0; j < matrix[i].length; j++) {
                System.out.print(matrix[i][j] + " ");
            }
            System.out.println();
        }
    }
}









memory allocation for object array

class Person {
    private String name;

    public Person(String name) {
        this.name = name;
    }

    public String getName() {
        return name;
    }
}

public class ObjectArrayMemoryAllocation {
    public static void main(String[] args) {
        Person[] people;

        people = new Person[4];

        people[0] = new Person("vijay");
        people[1] = new Person("siddharth");
        people[2] = new Person("Riyanka");
        people[3] = new person("shubham");
    
        System.out.println("Names of people:");
        for (int i = 0; i < people.length; i++) {
            System.out.println(people[i].getName());
        }
    }
}
